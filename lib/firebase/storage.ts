/**
 * Firebase Storage was removed because the project uses the Spark (free) plan
 * and Storage requires the Blaze (pay-as-you-go) plan.
 *
 * Poster uploads now go through ImgBB — see lib/imgbb.ts.
 */
